{
  "name": "qr-code-studio",
  "version": "2.0.0",
  "private": true,
  "description": "Private, offline, dependency-free QR code generator shipped as one standalone HTML file.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "lint": "node --check src/qr-core.js && node --check src/app.js && node scripts/lint.mjs",
    "test": "node test/qr-core.test.mjs && node test/repository.test.mjs",
    "check": "npm run build && npm run lint && npm test",
    "serve": "node scripts/serve.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
